package zork.thegame.controller;import java.util.ArrayList;/* * This class holds an enumeration of all command words known to the game. * It is used to recognise commands as they are typed in. * * This class is part of the "Zork" game. */import java.util.Arrays;class CommandWords {	// a constant array that holds all valid command words	private static final ArrayList<String> validCommands = new ArrayList<String>(			Arrays.asList("go", "quit", "help", "attack", "get", "inv", "examine",					"leave", "back", "map", "up", "down"));	/**	 * Constructor - initialise the command words.	 */	public CommandWords() {		// nothing to do at the moment...	}	/**	 * Check whether a given String is a valid command word. Return true if it	 * is, false if it isn't.	 **/	public boolean isCommand(String command) {		return validCommands.contains(command);	}	/*	 * Print all valid commands to System.out.	 */	public void showAll() {		for (String command : validCommands) {			System.out.print(command + "  ");		}		System.out.println();	}}